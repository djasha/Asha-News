# Directus Docker Configuration for VPS with Existing Services
# Copy this file to .env.directus and fill in your values

# Port Configuration (change if 8055 conflicts with existing services)
DIRECTUS_PORT=8055
# Alternative ports if 8055 is taken: 8056, 8057, 9055, etc.

# Database Configuration
DIRECTUS_DB_PASSWORD=your_secure_database_password_here

# Directus Security Keys (generate random strings)
DIRECTUS_SECRET_KEY=your_random_secret_key_32_chars_min
DIRECTUS_SECRET=your_random_secret_32_chars_min

# Admin User (first time setup)
DIRECTUS_ADMIN_EMAIL=admin@asha.news
DIRECTUS_ADMIN_PASSWORD=your_secure_admin_password

# Public Access
DIRECTUS_PUBLIC_URL=http://your-vps-ip:8055
# For production with domain: https://admin.asha.news

# CORS Configuration (your main website domains)
DIRECTUS_CORS_ORIGIN=http://localhost:3000,https://asha.news,https://www.asha.news

# Email Configuration (optional - for user invitations)
DIRECTUS_EMAIL_FROM=no-reply@asha.news
DIRECTUS_SMTP_HOST=
DIRECTUS_SMTP_PORT=587
DIRECTUS_SMTP_USER=
DIRECTUS_SMTP_PASSWORD=

# Notes:
# - Generate random strings for SECRET_KEY and SECRET using: openssl rand -hex 32
# - Replace your-vps-ip with your actual VPS IP address
# - If port 8055 conflicts, change DIRECTUS_PORT and update PUBLIC_URL accordingly
# - Data will be stored in ./directus-data/ directory (isolated from other services)
# - Resource limits: 1GB RAM max, 0.5 CPU cores max to prevent interference
